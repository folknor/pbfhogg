# API Documentation

The full API reference is generated by `cargo doc` and hosted here.

::: tip
In production, this page is replaced by the `cargo doc` output. The GitHub Actions workflow copies the generated docs into this directory at build time.
:::

For now, you can generate docs locally:

```sh
cargo doc --no-deps --open
```
